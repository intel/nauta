apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-skydns
  labels:
    addonmanager.kubernetes.io/mode: EnsureExists
    app: {{ .Release.Name }}-skydns
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
