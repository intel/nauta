apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ingress-defaultbackend
  labels:
    app: {{ .Release.Name }}-ingress-defaultbackend
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: {{ .Release.Name }}-ingress-defaultbackend
    release: {{ .Release.Name }}
