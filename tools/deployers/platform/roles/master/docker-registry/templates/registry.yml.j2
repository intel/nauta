version: 0.1
log:
  fields:
    service: registry
storage:
    cache:
        layerinfo: inmemory
    filesystem:
        rootdirectory: /opt/nauta/registry
    maintenance:
        readonly:
            enabled: true
http:
    addr: {{ nauta_configuration.internal_interface.ipv4_address }}:5000
    secret: REDACTED
    debug:
        addr: 127.0.0.1:5001
