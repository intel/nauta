Summary: NAUTA Local Yum Repository {{ VERSION_MAJOR }}.{{ VERSION_MINOR }}.{{ VERSION_NO }}-{{ VERSION_SUFFIX }}-{{ VERSION_ID }} package.
Name: nauta-yum
Version: {{ VERSION_MAJOR }}.{{ VERSION_MINOR }}.{{ VERSION_NO }}.{{ VERSION_SUFFIX }}
Release: {{ VERSION_ID }}
License: Apache-2.0
Group: Tools

%define  debug_package %{nil}

Requires: nauta-yum-repository = {{ VERSION_MAJOR }}.{{ VERSION_MINOR }}.{{ VERSION_NO }}.{{ VERSION_SUFFIX }}-{{ VERSION_ID }}

%description
%{summary}

%prep
# Empty

%build
# Empty section.

%install
rm -rf %{buildroot}
mkdir -p %{buildroot}/etc/yum.repos.d

echo "[NAUTARepository]
name=NAUTA repository
gpgcheck=0
enabled=0
baseurl=file:///opt/nauta/repository" > %{buildroot}/etc/yum.repos.d/nauta.repo

%clean
# Empty

%files
%defattr(0755,root,root,-)
/etc/yum.repos.d/nauta.repo
