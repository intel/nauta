Summary: DLS4E Local Yum Repository {{ VERSION_MAJOR }}.{{ VERSION_MINOR }}.{{ VERSION_NO }}-{{ VERSION_ID }} package
Name: dls4e-yum
Version: {{ VERSION_MAJOR }}.{{ VERSION_MINOR }}.{{ VERSION_NO }}
Release: {{ VERSION_ID }}
License: Apache-2.0
Group: Tools

%define  debug_package %{nil}

Requires: dls4e-yum-repository = {{ VERSION_MAJOR }}.{{ VERSION_MINOR }}.{{ VERSION_NO }}-{{ VERSION_ID }}

%description
%{summary}

%prep
# Empty

%build
# Empty section.

%install
rm -rf %{buildroot}
mkdir -p %{buildroot}/etc/yum.repos.d

echo "[DLS4ERepository]
name=DLS4Enterprise repository
gpgcheck=0
enabled=0
baseurl=file:///opt/dls4e/repository" > %{buildroot}/etc/yum.repos.d/dls4e.repo

%clean
# Empty

%files
%defattr(0755,root,root,-)
/etc/yum.repos.d/dls4e.repo
