---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: users.{{ .Values.api_group_name }}
  labels:
    app: {{ required "DLS4E Release name is required" .Values.global.dls4e }}-{{ .Chart.Name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    dls4e_release: {{ required "DLS4E Release name is required" .Values.global.dls4e }}
    dls4e_release_version: {{ required "DLS4E Release version is required" .Values.global.dls4e_release }}
spec:
  group: {{ .Values.api_group_name }}
  version: v1
  scope: Cluster
  names:
    plural: users
    singular: user
    kind: User
    shortNames:
    - u
  validation:
    openAPIV3Schema:
      properties:
        spec:
          properties:
            uid:
              type: integer
            state:
              type: string
              enum:
              - DEFINED
              - CREATED

