---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ required "DLS4E Release name is required" .Values.global.dls4e }}-access
  labels:
    app: {{ .Release.Name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    dls4e_release: {{ required "DLS4E Release name is required" .Values.global.dls4e }}
    dls4e_release_version: {{ required "DLS4E Release version is required" .Values.global.dls4e_release }}
rules:
  - apiGroups:
      - ""
    resources:
      - experiments
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - ""
    resources:
      - runs
    verbs:
      - get
      - list
      - watch
