# Default values for charts.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

TensorBoardPort: 8888
TensorIPythonPort: 6006

commandline:
  args:
    - "/app/training.py"
    - "--data_dir=/temp"

image:
  pullPolicy: IfNotPresent

service:
  type: ClusterIP

resources: {}
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  #requests: #TODO CHECK Tenserflow minimal requirements
  #  cpu: 100m
  #  memory: 128Mi
