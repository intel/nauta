Summary: Carbon CentOS7 yum package repository
Name: carbon-yum-repository
Version: {{ build_major }}.{{ build_minor }}.{{ build_no }}
Release: {{ build_id }}
License: Apache-2.0
Group: Tools
SOURCE0: package.tar.gz

BuildRoot: %{_tmppath}/%{name}-%{version}-%{release}-root

%define  debug_package %{nil}

%description
%{summary}

%prep
%setup -q -n repo

%build
# Empty section.

%install
rm -rf %{buildroot}
mkdir -p  %{buildroot}

mkdir -p %{buildroot}/opt/carbon/repository/
cp -a * %{buildroot}/opt/carbon/repository/

exit 0
%clean
rm -rf %{buildroot}


%files
%defattr(-,root,root,-)
/opt/carbon/repository/*
