---

- hosts: localhost
  connection: local
  gather_facts: False
  vars:
    ansible_python_interpreter: "{{ local_python_interpreter }}"
    terraform_file: "gcp/{{ prefix }}.tfstate"
    local_terraform_file: "{{ workspace }}/terraform/terraform.tfstate"
  roles:
    - role: terraform-clean
